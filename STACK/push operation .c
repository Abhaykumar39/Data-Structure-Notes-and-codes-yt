#include<stdio.h>
// we can also use #define processor directive to declare the constant .
# define max 4
int stack_arr[max];// global declaration of stack
int main()
{
     int top,s[top],data;
     if(top=max-1)
     {
        printf("stack is full\n");
     }
     else{
        printf("enter the element");
        top=top+1;
        s[top]=data;
     }
}
               